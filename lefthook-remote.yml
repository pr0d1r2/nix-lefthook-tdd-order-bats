---
pre-commit:
  commands:
    tdd-order-bats:
      run: timeout ${LEFTHOOK_TDD_ORDER_TIMEOUT:-30} lefthook-tdd-order-bats --staged

pre-push:
  commands:
    tdd-order-bats:
      run: timeout ${LEFTHOOK_TDD_ORDER_TIMEOUT:-30} lefthook-tdd-order-bats
